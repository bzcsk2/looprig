# Notice

This package incorporates work derived from
[agentmemory](https://github.com/rohitg00/agentmemory).

- Upstream commit: `749c280`
- License: Apache-2.0
- Modifications: Adapted for Deepreef's in-process runtime (`MemoryRuntimeSdk`
  replaces `iii-sdk`; `MemoryStore` replaces iii-engine KV; paths changed from
  `~/.agentmemory` to `~/.covalo/memory`).
